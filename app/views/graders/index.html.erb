<p id="notice"><%= notice %></p>

<h1>Listing Graders</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Email</th>
      <th>Grader type</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @graders.each do |grader| %>
      <tr>
        <td><%= grader.name %></td>
        <td><%= grader.email %></td>
        <td><%= grader.grader_type %></td>
        <td><%= link_to 'Show', grader %></td>
        <td><%= link_to 'Edit', edit_grader_path(grader) %></td>
        <td><%= link_to 'Destroy', grader, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Grader', new_grader_path %> |
<%= link_to 'Projects', projects_path %>
